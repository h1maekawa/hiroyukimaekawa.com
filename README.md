# Hiroyuki Maekawa Portfolio

このリポジトリには、ポートフォリオサイトの実装一式を格納しています。  
アプリ本体はリポジトリ直下（`src` / `package.json`）に配置しています。

## セットアップ

```bash
npm install
```

## 開発サーバー起動

```bash
npm run dev
```

## 本番ビルド

```bash
npm run build
```

ビルド成果物は `dist` に出力されます。
